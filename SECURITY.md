# Security

Do not report vulnerabilities in public issues. Contact the repository owner privately.

Never commit Supabase connection strings, secret keys, provider tokens, access tokens, database
exports, or user data. If a secret is exposed, revoke and rotate it before removing it from Git
history.

