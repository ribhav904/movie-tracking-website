# Contributing

Use a short-lived `feat/`, `fix/`, `chore/`, or `docs/` branch linked to a GitHub issue. Open a
pull request against `main`, keep migrations immutable after merge, and squash-merge after CI
passes. API changes must include tests and update the OpenAPI-facing schemas.

